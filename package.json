{
  "name": "rx-test",
  "version": "1.0.0",
  "description": "Playing around with React and RX",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "graphql": "^0.6.0",
    "react": "^15.0.2",
    "rx": "^4.1.0",
    "rx-react": "^0.3.0"
  }
}
